{
  "name": "apgms",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "services/*",
    "webapp",
    "shared",
    "worker",
    "tools/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "sbom": "cyclonedx-bom -o sbom.json",
    "sca": "npm audit --json > audit.json || true"
  },
  "devDependencies": {
    "@types/node": "^24.7.1",
    "prisma": "6.17.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "cyclonedx-bom": "file:tools/cyclonedx-bom"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@prisma/client": "6.17.1",
    "fastify": "^5.6.1",
    "zod": "^4.1.12"
  }
}
